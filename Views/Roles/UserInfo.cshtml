@model TaskManagementSystem.Models.ApplicationUser
@{
    ViewBag.Title = "UserInfo";
}

<h2>UserInfo</h2>

<ol>
    @foreach (var project in Model.ProjectUsers)
    {
        <li>@project.Id</li>
    }
</ol>
